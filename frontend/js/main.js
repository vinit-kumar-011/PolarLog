/* =========================================================
   POLARLOG - MAIN JAVASCRIPT
   Shared across pages
========================================================= */

document.addEventListener("DOMContentLoaded", () => {
  /*
   * Bootstrap handles the mobile navbar.
   *
   * This file is intentionally kept lightweight.
   * Shared functionality for Dashboard, Cargo,
   * Inventory etc. can be added here later.
   */
});
